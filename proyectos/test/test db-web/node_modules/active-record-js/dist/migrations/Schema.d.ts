export default class Schema {
    static createTable(table: string, fn: Function): Promise<boolean>;
    static drop(table: string): Promise<boolean>;
    static dropIfExists(table: string): Promise<boolean>;
    static hasColumn(table: string, column: string): Promise<boolean>;
    static hasTable(table: string): Promise<boolean>;
    private static run;
}
