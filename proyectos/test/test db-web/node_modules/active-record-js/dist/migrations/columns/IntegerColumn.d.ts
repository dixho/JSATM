import Column from './Column';
import ColumnOptions from './ColumnOptions';
interface IntegerColumnOptions extends ColumnOptions {
    autoIncrement?: boolean;
    signed?: boolean;
}
export default class IntegerColumn extends Column {
    protected autoIncrement: boolean;
    protected signed: boolean;
    constructor(name: string, type: string, options?: IntegerColumnOptions);
    toString(): string;
}
export {};
