import ConnectionOptions from './ConnectionOptions';
import Database from './Database';
export default class DB {
    private static active;
    private static databases;
    private static isQueryLogEnabled;
    private static logger;
    static beginTransaction(): Promise<any>;
    static commit(): Promise<any>;
    static create(options: ConnectionOptions): void;
    static enableQueryLog(): void;
    static getActiveDatabase(): Database;
    static getQueryLog(): Array<any>;
    static run(query: any): Promise<any>;
    static rollBack(): Promise<any>;
    static setActiveDatabase(name: string): Database;
}
