export interface PaginateObject {
    count: number;
    page: number;
    lastPage: number;
    data: any;
}
export interface PaginationOptions {
    limit: number;
    query: any;
    page: number;
}
export declare class Pagination {
    limit: number;
    query: any;
    page: number;
    constructor({ limit, query, page }: PaginationOptions);
    paginate(): Promise<PaginateObject>;
}
