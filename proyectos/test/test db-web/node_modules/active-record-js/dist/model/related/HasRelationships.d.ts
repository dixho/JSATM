import Builder from '../../query/Builder';
export default class HasRelationships {
    belongsTo(related: string, foreignKey: string, localKey: string): Builder;
    belongsToMany(related: string, pivot: string, foreignPivotKey: string, localPivotKey: string): Builder;
    hasMany(related: string, foreignKey: string, localKey: string): Builder;
    hasOne(related: string, foreignKey: string, localKey: string): Builder;
}
