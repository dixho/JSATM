"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class Relations {
    constructor() {
        this.relations = {};
    }
    isRelation(modelName, methodName) {
        if (!this.relations.hasOwnProperty(modelName) ||
            this.relations[modelName].indexOf(methodName) === -1) {
            return false;
        }
        return true;
    }
    getRelations(modelName) {
        if (!this.relations.hasOwnProperty(modelName)) {
            return [];
        }
        return this.relations[modelName];
    }
    setRelation(modelName, methodName) {
        if (!this.relations.hasOwnProperty(modelName)) {
            this.relations[modelName] = [];
        }
        this.relations[modelName].push(methodName);
    }
}
exports.default = new Relations();
