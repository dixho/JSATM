"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const mysql = __importStar(require("mysql"));
const constants_1 = require("../constants");
class Join {
    constructor(table, localKey, operator, foreignKey, type) {
        this.type = constants_1.JoinType.Inner;
        this.table = table;
        this.localKey = localKey;
        this.operator = operator;
        this.foreignKey = foreignKey;
        if (type) {
            this.type = type;
        }
    }
    toString() {
        return `${this.type} ${mysql.escapeId(this.table)} ON ${mysql.escapeId(this.localKey)} ${this.operator} ${mysql.escapeId(this.foreignKey)}`;
    }
}
exports.default = Join;
