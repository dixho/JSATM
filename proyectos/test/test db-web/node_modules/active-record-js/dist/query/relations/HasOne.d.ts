import Relation from './Relation';
export default class HasOne extends Relation {
    constructor(model: object, related: string, foreignKey: string, localKey?: string);
    buildQuery(): import("..").Builder;
}
